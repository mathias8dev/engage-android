# Engage In-App for Android

Optional in-app experience engine for `engage-core`. It evaluates synchronized campaigns locally,
arbitrates automatic overlays, and exposes shared embedded placement hosts for Views and Compose.

